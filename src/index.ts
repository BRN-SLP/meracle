/**
 * Placeholder entry point.
 *
 * tsc with the configured `include` glob (`src/**\/*`) requires at
 * least one input file. This empty module satisfies that until
 * `phase-0/agent-identity` lands the first real code.
 */
export {};
